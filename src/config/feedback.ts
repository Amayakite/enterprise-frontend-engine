/** 全站反馈默认值；只定义展示规则，不保存业务状态。 */
export const feedbackConfig = {
  /** 顶部轻提示的偏移量，单位 px。 */
  offset: 64,
  /** 全局轻提示同时显示上限；达到上限时释放最早一条，避免积压覆盖页面。 */
  maxVisible: 4,
  /** 完成类轻提示显示时间，单位 ms。 */
  successDuration: 2600,
  /** 非完成类轻提示显示时间，单位 ms；长消息由用户关闭。 */
  noticeDuration: 5000,
  /** 超过此字符数时仅展示摘要，完整内容通过详情查看。 */
  summaryLength: 100,
  /** 超过此行数时同样启用详情；语义严重度不由字数决定。 */
  summaryLines: 3,
  /** 标准业务共用文案；模块仅覆盖业务成功文案。 */
  messages: {
    saved: "保存成功",
    failed: "操作未完成，请检查提示后再试",
    unknown: "提交结果待核实，请勿重复提交；请联系对应人员确认处理结果。",
    committed: "数据已提交，仅页面数据未更新；请重试回填，不要重新建单。",
    postSave: "数据已保存，后续处理未完成；请核对当前页面或返回列表查看。",
  },
};
